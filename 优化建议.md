我看的是当前 `main` 分支。整体判断是：**这个项目已经不是“第一次写 Monte Carlo”的小实验了，核心能力其实已经有雏形，但现在最大的提升空间在“正确性约束 + 架构统一 + Monte Carlo 内存模型”，而不是继续堆产品类。** 当前同时存在两套 BS、两套 Monte Carlo、两代结构化产品实现，这正适合做一次系统重构。

## 一、我建议最先修的 6 个问题

### P0-1：统一利率与折现口径

这是我认为优先级最高的数值问题。

例如 ELN 中：

```python
r: float = np.log(1 + 0.015)
```

注释说明它被转换成了**连续复利收益率**，但折现却写成：

```python
1 / (1 + self.r) ** (self.T / 365)
```

如果 `r` 是连续复利，应当是：

```python
np.exp(-r * T_years)
```

这个模式同时出现在 ELN、BEN、Rainbow、Leverage Note。

建议从根上禁止“大家自己理解 r”：

```python
@dataclass(frozen=True)
class Market:
    risk_free_rate: float   # continuous annual rate
    dividend_yield: float   # continuous annual rate
```

整个项目统一：

```python
discount = np.exp(-r * year_fraction)
```

并在文档中明确：

> 所有 rate 均为年化连续复利；所有 volatility 均为年化；所有定价函数内部时间单位均为 year fraction。

------

### P0-2：Rainbow 的两个重要参数实际上没进入定价

`RainbowNote2` 定义了：

```python
lower_call_strike
upside_participation
```

但 `price()` 最终只根据 `put_strike`、`guaranteed_flat_coupon` 和 worst-of 表现计算，前两个参数完全没用。

也就是说现在：

```python
upside_participation=0.5
```

和：

```python
upside_participation=2.0
```

**可能得到完全相同的价格。**

这应该直接加入单元测试：

```python
assert price(participation=0.5) != price(participation=2.0)
```

更好的做法是把 payoff 单独写成纯函数，这种问题会很容易发现：

```python
def payoff(self, terminal_returns):
    ...
```

------

### P0-3：`AutoCallBEN` 目前并没有真正实现 AutoCall

现在它增加了：

```python
ki_barrier
autocall_barrier
autocall_frequency
```

但是没有覆盖 `price()` 或 payoff，因此这些参数并没有参与定价。

从类名来看很容易让调用者误以为：

> 已经支持敲入、敲出和观察日。

实际上没有。

我宁愿暂时：

```python
class AutoCallBEN:
    raise NotImplementedError
```

也不要返回一个“看起来合理但并非 autocall”的价格。

------

### P0-4：旧版和新版 payoff 存在口径不一致

例如旧 BEN 转股用了：

```python
nominal_amount // (put_strike * S0) * ST
```

也就是整数除法 `//`。

新版 BEN2 则基本相当于：

```python
ST / put_strike
```

允许连续数量。

这意味着 BEN 和 BEN2 即使参数完全一样，也未必是同一种合约。

如果合同确实要求整数股，那么还必须定义：

- 剩余现金怎么处理；
- 是否现金补差；
- 零碎股如何处理。

否则定价通常应该使用 `/` 而不是 `//`。

因此旧版/新版不是简单“性能不同”，而可能存在**产品定义不同**。

------

### P0-5：BS 有两套实现，应尽快合并

现在同时有：

- `eu_bs.py`
- `euro_option_bs.py`

而且口径已经开始分叉。

`eu_bs.py` 已经支持 generalized cost-of-carry：

```python
b
r
```

另一版只支持标准 BS。

此外第一版里已经出现：

```python
# TODO FIXME 极限情况可能错误
```

以及 Cash-or-Nothing theta 中：

```python
# 是否有 * self.option_type？？？
```

这类代码在金融定价库里应该视为高优先级，因为错误结果往往不会报错，而是输出一个正常-looking float。

我的建议不是继续修两套，而是只留下：

```text
BlackScholesModel
├── call
├── put
├── digital
└── greeks
```

------

### P0-6：目前其实没有真正的自动化测试

`option_pricing_test.py` 主要是：

```python
data = euro_option_bs(**kwargs)
print(data)
```

没有 `assert`。

另一个测试甚至：

```python
plt.show()
```

这会让 CI 测试变成图形交互程序。

对于定价项目，**测试的重要性甚至高于注释**。

------

# 二、Monte Carlo 是这个项目最值得大改的地方

你 README 里写的经验其实方向是对的：

> 内存足够时 NumPy 很好，多标的尽量向量化。

但是现在已经走到了另一个极端。

## 当前默认参数可能需要约 96 GB 一个数组

这里：

```python
get_stock_path_1y = partial(
    get_stock_path,
    u=250,
    m_step=240
)
```

默认：

```python
n_path=100000
```

再加 antithetic：

```python
epsilons = np.hstack([_half_ep, -_half_ep])
```

于是数组规模大约：

```text
60,001 个时间节点
× 200,000 条路径
× 8 bytes
≈ 96 GB
```

而计算过程中还有：

- `_half_ep`
- `epsilons`
- `multiplier`
- `stock_path`

所以峰值内存远不止 96 GB。

这里应该改变设计原则：

> **向量化不等于必须一次把所有路径放进内存。**

------

# 三、推荐的新 Monte Carlo 架构

## 1. Chunked Monte Carlo

例如：

```python
for paths in simulator.simulate_batches(
    n_paths=1_000_000,
    batch_size=20_000,
):
    payoff = product.payoff(paths)
    accumulator.update(payoff)
```

最后只保存：

```text
sum(payoff)
sum(payoff²)
n
```

即可得到：

- Price
- Standard Error
- 95% CI

这样一百万甚至一千万路径也可以跑，而内存基本固定。

------

## 2. 很多产品根本不需要完整路径

你目前的 ELN/BEN/Rainbow 很多 payoff 实际只看：

```python
ST_arr = relative_S[:, :, -1]
```

这种产品完全没必要模拟：

```text
S0
S1
S2
...
S60000
```

直接模拟 GBM 终值：

[
S_T=S_0\exp\left((r-q-\frac12\sigma^2)T+
\sigma\sqrt{T}Z\right)
]

即可。

于是：

```text
路径内存复杂度
O(paths × timesteps)

↓

O(paths)
```

这是整个项目潜在最大的性能优化。

### 对路径依赖产品

例如真正的 AutoCall，也没必要模拟“一天 240 个节点”。

只需要：

- observation dates；
- barrier monitoring dates；

如果需要连续障碍，再考虑 Brownian Bridge 修正。

------

# 四、多标的 Monte Carlo 应成为真正的一等公民

你的数组设计：

```text
X = underlying
Y = path
Z = time
```

这个思路挺清晰。

下一步建议正式加入：

```python
vol = np.array([...])

corr = np.array([
    [1.0, 0.6],
    [0.6, 1.0],
])

L = np.linalg.cholesky(corr)
Z_corr = L @ Z
```

否则 worst-of / rainbow 产品中：

> **相关系数很可能是估值最重要的风险参数之一。**

建议市场模型最终至少包含：

```python
MarketState(
    spots,
    rates,
    dividends,
    vols,
    correlation,
)
```

------

# 五、Greeks 可以比现在快很多

目前 BEN/Rainbow/ELN 的 Delta 大致是：

```text
price(S)
price(S + epsilon)
price(S - epsilon)
```

也就是一个 Delta 至少重新定价 2～3 次。

短期先保留 central difference 是可以的，但要做两件事。

第一，明确使用 **Common Random Numbers**。也就是：

```text
S
S+ε
S-ε
```

必须使用完全相同的随机数。

你现在因为复用了已有 `relative_S`，实际上已经具备一点这种性质，建议正式做成 engine 功能。

第二，逐步实现：

- pathwise Delta；
- likelihood-ratio Greeks；
- analytical Greeks。

特别是 LeverageNote 的 Delta 你实际上已经直接写成了解析值：

```python
delta = discount * leverage_multiple
```

这个方向是对的。

------

# 六、几个很容易做的 NumPy 优化

### `np.sort(... )[:, 0]` → `np.min(..., axis=1)`

现在 Rainbow/BEN 中有类似：

```python
worst_pnl = np.sort(ST_arr)[:, 0]
```

只是找最小值，不需要排序：

```python
worst = np.min(ST_arr, axis=1)
```

从：

```text
O(N log N)
```

变为：

```text
O(N)
```

标的数量不多时绝对耗时差异不大，但语义更准确。

### 少用 `hstack`

目前很多：

```python
s1 = ...
s2 = ...
s3 = ...
np.mean(np.hstack((s1, s2, s3)))
```

更推荐直接：

```python
payoff = np.where(
    condition1,
    payoff1,
    np.where(condition2, payoff2, payoff3),
)

return payoff.mean()
```

减少临时数组和数据复制。

------

# 七、建议重新设计整个类关系

目前基本是：

```text
产品 = payoff + MonteCarlo engine
```

例如：

```python
class RainbowNote(BaseOption, MonteCarlo)
```

我更推荐改成**组合而不是继承**：

```text
Product
   ↓
定义 payoff

Model
   ↓
定义风险中性过程

PricingEngine
   ↓
负责计算

Market
   ↓
提供 spot / vol / rate / correlation
```

最终调用类似：

```python
product = ELN(...)
market = MarketState(...)
model = GBM(...)
engine = MonteCarloEngine(...)

result = engine.price(product, model, market)
```

那么：

```python
engine.price(eln)
engine.price(rainbow)
engine.price(autocall)
```

复用一套 MC。

甚至可以：

```python
AnalyticEngine
MonteCarloEngine
PDEEngine
```

而产品定义完全不变。

------

# 八、我会把目录重构成这样

```text
src/fistqslab/
│
├── products/
│   ├── vanilla.py
│   ├── digital.py
│   ├── eln.py
│   ├── ben.py
│   ├── rainbow.py
│   └── autocall.py
│
├── models/
│   ├── black_scholes.py
│   └── gbm.py
│
├── engines/
│   ├── analytic.py
│   └── monte_carlo.py
│
├── market/
│   ├── state.py
│   ├── rates.py
│   └── calendar.py
│
├── risk/
│   └── greeks.py
│
├── results.py
└── api/
    └── flask_app.py
```

尤其是把 Flask 从定价核心中隔离出去。

现在 `app.py` 会把自然日 `T` 再 `/365` 后交给 BS。

这种单位转换最好不要偷偷放在 Web 层和核心层之间，而是明确：

```python
year_fraction = day_count.year_fraction(start, expiry)
```

------

# 九、测试体系应该这样建立

我会优先增加以下测试：

1. **BS benchmark**

   - Call/Put 对照已知结果。
   - Put-call parity。
   - Delta/Gamma/Vega/Theta/Rho 对照解析值。

2. **finite-difference test**

   ```python
   delta_fd = (V(S+h)-V(S-h))/(2*h)
   assert analytic_delta == approx(delta_fd)
   ```

3. **边界测试**

   - `T → 0`
   - `sigma → 0`
   - deep ITM
   - deep OTM
   - ATM

4. **Monte Carlo vs BS**
   对欧式期权：

   ```text
   MC price ≈ BS price
   ```

   最好判断：

   ```text
   |MC-BS| < 3 × standard_error
   ```

5. **结构化产品手算场景**
   每个产品人为构造 5～10 条确定路径，分别覆盖：

   ```text
   大涨
   小涨
   平盘
   小跌
   触及 strike
   大跌
   敲入
   敲出
   ```

6. **性质测试**
   例如 vanilla call：

   ```text
   spot ↑ => call price 不下降
   vol ↑ => call price 不下降
   ```

这些测试对金融代码非常有价值，因为很多 bug 不会 crash，只会给你一个错了几十 bp 的数字。

------

# 十、API 和数据结构也值得改

现在 `BaseOption` 中：

```python
@property
def price(self):
    raise NotImplemented
```

这里应该至少是：

```python
raise NotImplementedError
```

更建议真正使用：

```python
@abstractmethod
```

另外结果不要只返回 float。

建议统一：

```python
@dataclass(frozen=True)
class PricingResult:
    price: float
    stderr: float | None = None
    delta: np.ndarray | None = None
    gamma: np.ndarray | None = None
    vega: np.ndarray | None = None
    n_paths: int | None = None
    seed: int | None = None
```

对 Monte Carlo 来说尤其重要。

**一个 MC price 没有 standard error，其实是不完整的结果。**

------

# 十一、随机数接口应该升级

现在：

```python
np.random.normal(...)
```

使用全局 RNG。

推荐：

```python
rng = np.random.default_rng(seed)

z = rng.standard_normal(...)
```

这样才能保证：

- 可复现；
- 单元测试稳定；
- 并行计算不互相污染；
- CRN Greeks；
- benchmark 可比较。

之后还可以加入：

```text
Pseudo Random
Antithetic
Sobol / Quasi Monte Carlo
```

作为可配置策略。

------

# 十二、文档目前是明显短板

README 现在基本还是一篇“开发心得”，主要介绍 NumPy、迭代器和数组维度。

而 `pyproject.toml` 里的：

```toml
description = ""
```

开发依赖也基本只有 Black 和 isort。

我会把 README 至少扩成：

```text
FISTQSlab
│
├─ 项目是什么
├─ 支持的产品
├─ 支持的定价模型
├─ 安装
├─ Quick Start
├─ 利率/波动率/时间单位约定
├─ Black-Scholes 示例
├─ Monte Carlo 示例
├─ ELN/BEN/Rainbow 示例
├─ Greeks
├─ Monte Carlo 精度说明
├─ 项目架构
└─ 已知限制
```

另外，每个结构化产品最好直接写 payoff 数学公式。

比写：

```python
# 最差标的低于 put_strike
```

更有价值的是：

[
Payoff =
\begin{cases}
...
\end{cases}
]

这样几年以后重新看代码，也可以判断代码到底实现了什么。

------

# 十三、代码质量工具

可以增加：

```text
pytest
pytest-cov
ruff
mypy 或 pyright
pre-commit
GitHub Actions
```

并把：

```python
func_other_params={}
```

这种 mutable default 改掉。

更重要的是 `eu_bs.py` 里的：

```python
exec(draw_other_lines)
```

建议彻底移除，改成 callback：

```python
draw_callback(ax)
```

这样安全性、IDE 支持和可维护性都会好很多。

------

# 十四、如果由我来实际改这个项目，我会按这个顺序

| 优先级 | 改动                             | 收益                 |
| ------ | -------------------------------- | -------------------- |
| **P0** | 修正 rate / discount convention  | 防止系统性估值偏差   |
| **P0** | 修 Rainbow / AutoCall 未使用参数 | 防止产品定价错误     |
| **P0** | 建立 benchmark 单元测试          | 给后续重构安全网     |
| **P0** | 明确 T/r/sigma 单位              | 防止 365 倍级别错误  |
| **P1** | 合并两套 BS                      | 消除重复逻辑         |
| **P1** | 合并 MonteCarlo / MonteCarlo2    | 降低维护成本         |
| **P1** | Product / Model / Engine 分离    | 建立长期架构         |
| **P1** | MC chunking                      | 大幅降低内存         |
| **P1** | terminal-only 产品不保存路径     | 可能是数量级提升     |
| **P2** | MC stderr / CI / seed            | 提升结果可信度       |
| **P2** | correlated GBM                   | 完善多资产产品       |
| **P2** | pathwise Greeks / CRN            | 大幅提升 Greeks 性能 |
| **P2** | Sobol / control variate          | 提升 MC 收敛         |
| **P3** | README + 产品公式                | 可维护性             |
| **P3** | Ruff / typing / CI               | 工程质量             |

## 最关键的一点

我不会把这个项目的下一阶段目标定义成：

> “优化现有 Python 代码，让它更快。”

我会定义成：

> **把 FISTQSlab 从“若干可以跑的定价脚本”重构成一个有明确产品定义、统一市场参数、可验证数值结果和高效 Monte Carlo engine 的小型衍生品定价库。**

你的 NumPy 向量化方向本身是对的；现在更需要补上的，是**产品—模型—引擎三层分离、数值测试和 Monte Carlo 内存/误差管理**。做到这一步后，再考虑 Numba/Cython/Rust 才真正有意义。