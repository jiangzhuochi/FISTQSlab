# FISTQSlab 重构优化报告

对应 [`优化建议.md`](优化建议.md) 的 **P0 + P1** 两档执行完成。
本报告逐条记录：问题 → 改动 → 验证结果。

- **范围**：P0（正确性）+ P1（架构重构）+ 引擎核心契约（seed/stderr/CI，属 MC 引擎必需）
- **未做项**（P2/P3）：见文末"后续可做"清单

---

## 一、数值口径统一（P0-1）

### 问题

旧代码 7 处 `r = np.log(1 + 0.015)`（连续复利）搭配 `1 / (1 + r) ** (T / 365)`
（定期复利折现），口径自相矛盾；另有 `T * 250 // 365` 交易日换算、365/250
混用。

### 改动

- 新增 `market/day_count.py`：`year_fraction(days) = days/365`（Act/365）；
- 全库统一折现 `np.exp(-r * T)`，删除所有 `1/(1+r)**(T/365)`；
- 删除 `T * 250 // 365` 交易日换算，时间一律 year fraction；
- `MarketState` 集中承载 `risk_free_rate`（年化连续复利）与 `dividend_yields`
  （年化连续复利），定价函数不再接收散落的 `r`；
- 新增 `tests/test_conventions.py::test_discount_is_continuous_compounding`
  固化口径（断言连续复利与旧定期复利相差 > 1e-5）。

### 验证

- `test_conventions.py`：折现口径、year_fraction 往返、MarketState 广播与校验
- 所有产品 MC 与闭式在统一口径下吻合（见第三节）

---

## 二、Rainbow 未用参数（P0-2）

### 问题

`RainbowNote2.lower_call_strike` / `upside_participation` 完全不参与
`price()`——`participation=0.5` 与 `2.0` 价格相同。

### 改动

`products/rainbow.py` 将二者真正纳入 payoff：

```
payoff = 1 + coupon + participation·(worst − lower_call_strike)⁺ − (put_strike − worst)⁺ / put_strike
```

并校验 `lower_call_strike ≥ put_strike`。

### 验证

`tests/test_products.py`：

- `test_rainbow_upside_participation_affects_price`：
  `participation=0.5` 与 `2.0` 价格不同，且参与率越高价格越高；
- `test_rainbow_lower_call_strike_affects_price`：
  `lower_call_strike↑` → 价格↓。

示例实测（三标的，相关系数矩阵）：

```
参与率=0.5: 价格 = 0.979480
参与率=1.0: 价格 = 0.990666
参与率=2.0: 价格 = 1.013038
```

---

## 三、AutoCall 真正实现（P0-3）

### 问题

`AutoCallBEN` 是空壳：`ki_barrier` / `autocall_barrier` / `autocall_frequency`
只存储不参与定价，`price()` 仍是到期 BEN payoff。

### 改动

新增 `products/ben.py::AutoCallNote`，真实实现观察日自动赎回：

- 观察日按 `observation_frequency_days` 等间隔生成（year fraction，含到期日）；
- 首个观察日 `worst ≥ autocall_barrier` → 提前赎回，
  支付 `1 + coupon·(t_k/T)`（按时间 prorata），并折现 `exp(−r·t_k)`；
- 从未敲出：到期 `worst ≥ ki_barrier` 付 `1 + coupon`；
  否则转股 `max(min_redemption, worst / ki_barrier)`；
- 引擎按观察日分发到 `payoff_paths`（`GBMModel.path_batches` 只在观察日
  布点，用 Brownian 增量叠加，而非逐日 240 节点）。

### 验证

- `test_autocall_payoff_handcrafted`：4 条确定路径手算（敲出于 obs0 /
  敲出于 obs1 / 到期敲入转股 / 到期正常赎回）与 `payoff_paths` 逐位一致；
- `test_autocall_observation_days`：观察日 [30, 60, …, 360, 365] 正确生成；
- 示例 `examples/autocall.py`：50 万路径价格 `1.011019 ± 0.000035`。

---

## 四、两套 BS 合并（P0-5）

### 问题

同时存在 `eu_bs.py`（generalized b=r−q）与 `euro_option_bs.py`（标准 BS），
T 的单位注释自相矛盾（"自然日"却当年用）；`eu_bs.py` 含 `exec()`、
可变默认参数 `func_other_params={}`、`raise NotImplemented`、digital theta
存疑注释。

### 改动

合并为 `models/black_scholes.py` 纯函数模块：

- `bs_call` / `bs_put` / `bs_digital_call` / `bs_digital_put` /
  `bs_greeks`（price/delta/gamma/vega/theta/rho），支持 `q`（b = r − q）；
- 全部支持向量化（S 可为数组）；`T` 一律 year fraction；
- 删除 `exec()`（绘图函数整体移除，绘图职责交给示例）；
- digital 公式经手工推导验证：delta/gamma/vega/theta 的
  `option_type` 乘子均正确（`n(d2·option_type) = n(d2)` 因正态偶对称），
  其中 **digital 的 rho 修正**：`∂/∂r` 除 `−T·price` 外还有
  `cash·e^{−rT}·n(d2)·√T/σ` 项（旧实现无 rho）。

### 验证

`tests/test_black_scholes.py`（16 项）：

- 基准值：`C(100,100,1,0.05,0.2) = 10.4505835722`，`P = 5.5735260223`
- put-call parity（4 组参数）
- 希腊字母 vs 中心差分（含 digital 全部五项）
- 参考希腊值、边界（T→0 / σ→0 / ITM/OTM）、单调性质、股息影响

---

## 五、测试体系从零建立（P0-6）

### 问题

旧 `tests/` 无任何 `assert`、无 `test_*` 函数，另一个文件直接 `plt.show()`
阻塞。

### 改动

`pyproject.toml` 增加 `[tool.pytest.ini_options]`（`pythonpath=["src"]`），
dev 依赖增加 pytest / pytest-cov / ruff；重写测试体系（62 项全绿）：

| 文件 | 覆盖 |
| --- | --- |
| `test_black_scholes.py` | BS 基准 / parity / Greeks vs FD / 边界 / 性质 / digital |
| `test_conventions.py` | 折现口径 / 时间单位 / MarketState / GBM 分布与相关性 |
| `test_monte_carlo.py` | MC vs BS（3σ）/ 分块=单批 / seed 复现 / 收敛 / 1M 路径冒烟 |
| `test_products.py` | 手造确定路径 payoff / MC vs 闭式 / 未用参数回归 / AutoCall / 反解 |
| `test_greeks.py` | FD vs 解析 / CRN 复现 / Leverage delta / 多标的形状 |

---

## 六、Monte Carlo 架构重写（P1）

### 问题

旧 `get_stock_path` 默认参数（`u=250, m_step=240, n_path=100000` + 对偶变量
hstack）产生约 `60001 × 200000` 的数组 ≈ **96 GB/个**，峰值 336 GB；
且整条路径（6 万节点）被生成，而 ELN/BEN/Rainbow/Leverage 只看终值。

### 改动（`models/gbm.py` + `engines/monte_carlo.py`）

1. **分块生成**：`terminal_batches` / `path_batches` 以 `batch_size`
   产出固定大小批次，引擎用 `_Accumulator`（仅 `sum / sum² / n`）
   累加 → **内存与总路径数无关**；
2. **terminal-only**：只看终值的产品直接模拟
   `S_T = S0·exp((r−q−σ²/2)T + σ√T·Z)`，内存 O(路径) 而非 O(路径×时间步)；
3. **路径依赖**：AutoCall 只在观察日布点（Brownian 增量叠加）；
4. **多标的**：`MarketState.correlation` → Cholesky → `L·Z`；
5. **可复现**：`np.random.default_rng(seed)`，同 seed 逐位一致；
6. **对偶变量**：`n_paths` 独立抽样，有效路径 `2×n_paths`；
7. **结果完整**：`PricingResult` 含 `price / stderr / ci_low / ci_high /
   n_paths / n_effective_paths / seed`。

### 验证

- `test_million_paths_smoke`：100 万路径 + batch 20k 内存恒定秒级完成，
  MC vs BS < 3σ；
- `test_chunked_equals_single_batch`：同 seed 分块与单批**逐位一致**；
- `test_seed_reproducibility` / `test_more_paths_reduces_stderr`；
- `test_gbm_correlation_structure`：模拟相关性 0.6007 ≈ 0.6；
- 示例 ELN 100 万路径：`0.982796 ± 0.000024` vs 闭式 `0.982800`。

---

## 七、产品—模型—引擎分层（P1）

### 改动

```
src/fistqslab/
├── market/      MarketState + day_count（Act/365）
├── models/      GBMModel（分块）+ black_scholes（解析）
├── products/    base.Product + vanilla / eln / ben / rainbow / leverage
├── engines/     MonteCarloEngine / AnalyticEngine
├── risk/        GreeksCalculator（FD + CRN）
├── results.py   PricingResult
└── app.py       Flask API
```

调用模式：`engine.price(product, model)`——同一引擎定价所有产品；
`AnalyticEngine` 调度 `product.analytic_price(market)` 作对照。
旧 `option_pricing/`、`data_display/`、`example/`、`example2/` 已删除，
示例统一迁移到 `examples/`（7 个脚本，均以新 API 演示闭式 vs MC）。

### 旧 → 新 API 映射

| 旧 | 新 |
| --- | --- |
| `eu_bs.EuropeanCallOption(S, L, T_years, b, r, sigma)` | `bs_call(S, K, T, r, sigma, q)` / `bs_greeks("call", …)` |
| `euro_option_bs.euro_option_bs(**kwargs)` | `bs_call/bs_put/bs_greeks` |
| `ELN2(strike, issue_price, r, …)` + `relative_S` 数据 | `ELN(strike, maturity_year_fraction, issue_price)` + `engine.price(eln, model)` |
| `RELN2` | `RELN(strike, issue_price, maturity_year_fraction)` |
| `BaseBEN2` / `BaseBEN` | `BonusEnhancedNote(put_strike, coupon_barrier, bonus_coupon, maturity_year_fraction, min_redemption)` |
| `AutoCallBEN`（空壳） | `AutoCallNote(ki_barrier, autocall_barrier, observation_frequency_days, …)` |
| `RainbowNote2`（参与率无效） | `RainbowNote(put_strike, lower_call_strike, upside_participation, …)` |
| `LeverageNote2` | `LeverageNote(leverage_multiple, maturity_year_fraction, dividend_rate)` |
| `get_stock_path(...)`（96GB） | `GBMModel(...).terminal_batches(T, n_paths, batch_size, seed)` |
| `MonteCarlo` / `MonteCarlo2` | `MonteCarloEngine(n_paths, batch_size, antithetic, seed)` |
| `app.py`（`T/=365` 隐藏转换 + 死 import） | 显式 `year_fraction(T_days)`，返回 price + greeks |

### 口径/语义变化（有意为之，勿当作回归）

- 折现 `1/(1+r)^(T/365)` → `exp(−r·T)`（差数 bp，属修正）；
- 删除 `nominal_amount` 与整数股 `//` 转股，payoff 一律按**单位名义本金**
  计（连续份额，P0-4）；
- 票息改为**整个期限的总票息**（沿用 `example2/` 闭式语义）；
- `LeverageNote` 的 delta：旧 `discount·leverage_multiple` 与其 payoff
  （只依赖相对收益）矛盾；新实现 payoff 对绝对 spot 的导数为 0，
  `risk/greeks.py` 的 FD 结果与之一致；
- `AutoCallBEN` 由"假 autocall"变为真正的 `AutoCallNote`（P0-3）。

---

## 八、Greeks（P1）

`risk/greeks.py::GreeksCalculator`：对每标的 ±ε bump **相同 seed 重定价**
（CRN），`delta[i] = (V(S+ε)−V(S−ε))/(2ε·S0[i])`（对绝对价格的导数）。

验证：`test_greeks.py` —— vanilla delta/gamma vs 解析（容差内）、
同 seed 逐位复现、Leverage delta ≈ 0、多标的形状正确。

---

## 九、Web 层与依赖（P1）

- `app.py`：时间转换显式化（`year_fraction(T_days)`），删除死 import
  （`json` / `DataFrameJSONEncoder` / pydantic 模型），移除指向已删模块的
  `/data_display/reits` 与模板路由；新增 `option`/`q` 参数；
  返回 price + 希腊字母。test client 验证：call(365d)=10.4506、缺参 400；
- `pyproject.toml`：补 `description`、版本 0.2.0、python 约束 `>=3.10,<3.12`、
  dev 依赖 pytest/pytest-cov/ruff、`[tool.pytest.ini_options]`、`[tool.ruff]`；
  移除已无引用的依赖（pydantic/numexpr/nptyping/more-itertools/rich）。

---

## 十、内存与性能对比

| 项 | 旧 | 新 |
| --- | --- | --- |
| 100 万路径峰值内存 | ~96 GB/数组，峰值 336 GB | 数十 MB（与 batch_size 相关，与路径数无关） |
| terminal-only 产品 | 模拟 60,001 节点整条路径 | 直接模拟 S_T（O(路径)） |
| 路径依赖产品 | 逐日 240 节点 | 只在观察日布点 |
| 随机数 | 全局 `np.random`，不可复现 | `default_rng(seed)`，逐位复现 |
| 结果 | 裸 float | price + stderr + 95% CI + seed |

---

## 十一、后续可做（P2/P3，本次未做）

- **P2**：pathwise / likelihood-ratio Greeks（当前为 FD+CRN）；
  Sobol / quasi-MC、control variates；相关 GBM 的多标的闭式（多元正态积分）；
- **P3**：GitHub Actions CI、pre-commit、mypy 配置；
  交易日历（当前观察日按自然日等间隔）。

## 验证命令

```bash
pytest                    # 62 项全绿
ruff check src tests      # 全过
python examples/*.py      # 7 个示例均可运行（闭式 vs MC 对照）
```
